[package]
name = "rotex-core"
version = "0.1.2"
edition = "2024"
license = "MIT"
description = "GpuBackend trait and GraphicsContext adapter interface for Rotex engine backends"
repository = "https://github.com/rotexengine/rotex-core"

[dependencies]
thiserror = "2.0.18"
raw-window-handle = "0.6.2"
rotex-types = { version = "0.1.1" }